cmd parseint do
  arg val: str
  flag fallback: int = 0

  # inline
  n = try to_int(val) rescue err => fallback

  # bloque
  detail = try do
    parsed = to_int(val)
    "ok: #{parsed}"
  rescue err do
    "fail: #{err}"
  end

  "n=#{n}" |> print
  detail |> print
end
