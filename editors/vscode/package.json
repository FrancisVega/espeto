{
  "name": "vscode-espeto",
  "displayName": "Espeto",
  "description": "Language support for Espeto: syntax highlighting, hover docs, go-to-definition, completion, references, rename, outline, folding, signature help, and semantic tokens.",
  "version": "0.2.0",
  "publisher": "hisco",
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onLanguage:espeto"
  ],
  "scripts": {
    "build:server": "cd ../.. && pnpm build:manifest && esbuild src/lsp/server.ts --bundle --platform=node --target=node18 --format=cjs --outfile=editors/vscode/dist/lsp.js",
    "build:client": "esbuild src/extension.ts --bundle --platform=node --target=node18 --format=cjs --outfile=dist/extension.js --external:vscode",
    "build": "pnpm build:server && pnpm build:client",
    "typecheck": "tsc --noEmit"
  },
  "contributes": {
    "languages": [
      {
        "id": "espeto",
        "aliases": [
          "Espeto",
          "espeto"
        ],
        "extensions": [
          ".esp"
        ],
        "filenames": [
          "moraga.lock"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "espeto",
        "scopeName": "source.espeto",
        "path": "./syntaxes/espeto.tmLanguage.json"
      }
    ],
    "semanticTokenScopes": [
      {
        "language": "espeto",
        "scopes": {
          "function": ["entity.name.function.espeto"],
          "function.defaultLibrary": ["support.function.builtin.espeto"],
          "parameter": ["variable.parameter.espeto"],
          "variable": ["variable.other.espeto"],
          "variable.defaultLibrary": ["variable.language.espeto"]
        }
      }
    ]
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/vscode": "^1.118.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  }
}
