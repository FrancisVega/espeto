{
	"name": "espeto",
	"version": "0.2.1",
	"description": "Pipe-centric DSL for building CLIs.",
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=20"
	},
	"bin": {
		"espeto": "./dist/cli.js"
	},
	"files": [
		"dist",
		"README.md",
		"LICENSE"
	],
	"scripts": {
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit",
		"build:manifest": "tsx scripts/build-manifest.ts",
		"build": "pnpm build:manifest && esbuild src/cli.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/cli.js --banner:js='#!/usr/bin/env node' && chmod +x dist/cli.js && esbuild src/run.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/runtime.js && esbuild src/lsp/server.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/lsp.js --banner:js='#!/usr/bin/env node\nimport{createRequire}from\"node:module\";const require=createRequire(import.meta.url);' && chmod +x dist/lsp.js",
		"prepublishOnly": "pnpm build && ESPETO_OFFLINE=1 pnpm test",
		"espeto": "tsx src/cli.ts"
	},
	"packageManager": "pnpm@10.23.0",
	"devDependencies": {
		"@types/node": "^25.6.0",
		"esbuild": "^0.28.0",
		"tsx": "^4.21.0",
		"typescript": "^6.0.3",
		"vitest": "^4.1.5",
		"vscode-languageserver": "^9.0.1",
		"vscode-languageserver-textdocument": "^1.0.12"
	}
}
